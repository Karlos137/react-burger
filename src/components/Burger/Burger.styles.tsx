// Styled Components
import styled from "styled-components"

// Framer Motion
import { motion } from "framer-motion"

export const StyledBurger = styled(motion.svg)`
  cursor: pointer;
`

export const Line = styled(motion.line)``
